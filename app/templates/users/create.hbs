<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3 login-extend">
      <h3 class="text-center tittle-login">Crea tu cuenta Gratis</h3>
      <p class="small text-center txt-barcelona">
        Teniendo tu cuenta, podrás leer la Revista Barcelona, acceder desde
        cualquier dispositivo, teniendo todo en un solo lugar.
      </p>
      {{#if signup-error}}
          <span class="login-error">{{signup-error}}</span>
      {{/if}}
      <form class="register-form" role="form" {{action "create" on="submit"}}>
          <div class="form-group">
            <label class="small" for="InputEmail">Ingrese su correo</label>

              {{input type="email" value=username id="username" placeholder="hola@ejemplo.com" name="username" class="form-control"}}

          </div>
          <div class="form-group">
            <label class="small" for="InputPassword">Contraseña</label>

            {{input type="password" value=password id="password" placeholder="Contraseña" name="password" class="form-control"}}

          </div>
          <div class="form-group">
            {{!-- <label class="small" for="InputPassword">Repita su contraseña</label> --}}

            {{!-- aca se tendria que modificar para el que verifica que las contraseñas sean iguales --}}
            {{input type="password" value=confirm_password id="confirm_password" placeholder="Repita su contraseña" name="confirm_password" class="form-control"}}

          </div>
          {{kd-ladda-button status=isSending label="Registrarte"}}
        </form>
        <span class="signup text-center block">
          {{#link-to 'users.login'}}Ingresar con una cuenta existente{{/link-to}}
        </span>
    </div>
  </div>
</div>
